# SMTP Configuration (Backend)
# Fill these values to enable real email sending. 
# If left empty, the server will log messages to the console for testing.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
CONTACT_TO_EMAIL=info@unicornpetro.co.in

# Frontend Configuration
VITE_BACKEND_URL=http://localhost:3001
